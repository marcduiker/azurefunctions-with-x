CREATE TABLE movies (
    id           SERIAL PRIMARY KEY,
    title        VARCHAR(100) NOT NULL,
    release_year INTEGER NOT NULL
);

--
SELECT * FROM movies

--
INSERT INTO movies (title, release_year) VALUES ('The Little Mermaid', 1989)